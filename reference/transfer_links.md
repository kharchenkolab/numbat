# Annotate the direct upstream or downstream node on the edges

Annotate the direct upstream or downstream node on the edges

## Usage

``` r
transfer_links(G)
```

## Arguments

- G:

  igraph Mutation graph

## Value

igraph Mutation graph
