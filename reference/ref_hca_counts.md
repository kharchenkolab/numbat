# reference expression counts from HCA

reference expression counts from HCA

## Usage

``` r
ref_hca_counts
```

## Format

An object of class `matrix` (inherits from `array`) with 24857 rows and
12 columns.
