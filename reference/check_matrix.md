# Check the format of a count matrix

Check the format of a count matrix

## Usage

``` r
check_matrix(count_mat)
```

## Arguments

- count_mat:

  matrix Count matrix

## Value

matrix Count matrix
