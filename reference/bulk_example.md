# example pseudobulk dataframe

example pseudobulk dataframe

## Usage

``` r
bulk_example
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with
3935 rows and 83 columns.
